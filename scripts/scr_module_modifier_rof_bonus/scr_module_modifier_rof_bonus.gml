function scr_module_modifier_rof_bonus(argument0) {
	// Description text
	value = argument0
	//modifier_description = ("Rate of fire +" + string(abs(value)))

	// Modifier script

	bullet_interval_bonus += value


}
